<Window x:Class="FlashTelemetry.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Base (CTRL UDP 5001)" Height="650" Width="1100"
        WindowStartupLocation="CenterScreen">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Top bar -->
        <DockPanel Grid.Row="0" LastChildFill="True">
            <StackPanel Orientation="Horizontal" DockPanel.Dock="Left">
                <Button Name="BtnStart" Content="Start CTRL" Width="120" Margin="0,0,8,0" Click="BtnStart_Click"/>
                <Button Name="BtnStop" Content="Stop" Width="120" Click="BtnStop_Click"/>
            </StackPanel>

            <StackPanel Orientation="Horizontal" DockPanel.Dock="Right">
                <TextBlock Text="Perfil activo: " VerticalAlignment="Center" Margin="0,0,6,0"/>
                <TextBlock Name="TxtActiveProfile" FontWeight="Bold" VerticalAlignment="Center" Margin="0,0,20,0"/>

                <TextBlock Text="Último ESP32: " VerticalAlignment="Center" Margin="0,0,6,0"/>
                <TextBlock Name="TxtLastEsp" FontWeight="Bold" VerticalAlignment="Center"/>
            </StackPanel>
        </DockPanel>

        <!-- Profile chooser -->
        <GroupBox Grid.Row="1" Header="Cambiar perfil activo (manual, para pruebas)" Margin="0,10,0,10">
            <Grid Margin="10">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="300"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <ComboBox Name="CmbProfiles" Grid.Column="0" Height="28" />

                <Button Name="BtnApplyProfile" Grid.Column="1" Content="Aplicar" Width="120" Margin="10,0,0,0"
                        Click="BtnApplyProfile_Click"/>

                <TextBlock Name="TxtApplyResult" Grid.Column="2" Margin="10,0,0,0" VerticalAlignment="Center"/>
            </Grid>
        </GroupBox>

        <!-- Log -->
        <TextBox Name="TxtLog" Grid.Row="2"
                 FontFamily="Consolas"
                 VerticalScrollBarVisibility="Auto"
                 HorizontalScrollBarVisibility="Auto"
                 IsReadOnly="True"
                 TextWrapping="NoWrap"/>
    </Grid>
</Window>
